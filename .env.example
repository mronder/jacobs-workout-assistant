# Supabase (public — safe for client-side, protected by Row Level Security)
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# OpenAI (secret — server-side only, set in Cloudflare Dashboard for production)
# For local dev: Wrangler reads this from .env.local as well.
OPENAI_API_KEY=""
